{% extends 'base.html.twig' %}

{#{% form_theme form 'bootstrap_4_layout.html.twig' %}#}

{% block body %}
    <div class="container">
        <div class="jumbotron">
            <h1 class="display-4">{{ title }}</h1>
            <p><a href="{{ path('default') }}">Home</a></p>
        </div>
        <div class="card">
            <div class="card-body">
                <div id="task" data-task="{{ task }}"></div>
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('dist/bootstrap_legacy.css') }}">
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('dist/form_react.js') }}"></script>
{% endblock %}

{% block title %}
    {{ title }}
{% endblock %}

